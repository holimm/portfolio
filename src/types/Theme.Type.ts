export enum Theme {
  Default = 'default',
  Dark = 'dark',
  Light = 'light',
}

export enum ThemeElement {
  Default = 'default',
  Dark = 'dark',
  Light = 'light',
}

export type ThemeTypes = 'default' | 'light' | 'dark';
